@model UPCNETSystemCliente.ViewModel.Panel._AddEditCursoClase   
@{
    ViewBag.Title = "Usuarios";
    Model.Modo = Model.IDCurso == "" ? "Nuevo" : "Editar";
}
<form role="form" method="post" action="@Url.Action("AddEditCursoClase","Panel")">
    <div class="form-group">
        <label>Modo</label>
        @Html.TextBoxFor(x => x.Modo, new { @class = "form-control", @readonly = "readonly" })
    </div>
    <div class="form-group">
        <label>Curso</label>
        @Html.DropDownListFor(x => x.objCursoClase.IDCurso, Model.selectCursos)
    </div>
    <div class="form-group">
        <label>IDClase</label>
        @Html.TextBoxFor(x => x.objCursoClase.IDClase, new { @class = "form-control" })
    </div>
    <div class="form-group">
        <label>Dia</label>
        @Html.TextBoxFor(x => x.objCursoClase.Dia, new { @class = "form-control" })
    </div>
    <div class="form-group">
        <label>Hora Inicio</label>
        @Html.TextBoxFor(x => x.objCursoClase.HoraIni, new { @class = "form-control" })
    </div>
    <div class="form-group">
        <label>Hora Fin</label>
        @Html.TextBoxFor(x => x.objCursoClase.HoraFin, new { @class = "form-control" })
    </div>

    <button type="submit" class="btn btn-primary">
        <i class="fa fa-save"></i> Guardar
    </button>
    <a href="@Url.Action("MantenimientoCurso","Panel")" class="btn btn-default">
        <i class="fa fa-arrow-circle-o-left"> Cancelar</i>
    </a>
</form>